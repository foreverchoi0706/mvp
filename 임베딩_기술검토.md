# SEEK 로컬 임베딩 AI 후보 검토서

**작성 기준일:** 2026.09.22  
**대상 프로젝트:** SEEK  
**목적:** 사진 ↔ 개념 의미 유사도 점수 산정을 위한 상용 가능한 로컬 임베딩 모델 선정

---

# 1. 요구 조건

SEEK의 핵심 점수 엔진은 다음 조건을 만족해야 한다.

1. **사진과 텍스트를 같은 임베딩 공간에서 비교 가능**
2. **완전 로컬 실행 가능**
3. **iOS / Android 양쪽 지원 가능**
4. **상업적 사용 가능**
5. **앱에 탑재할 수 있는 크기**
6. **모바일에서 충분한 추론 속도**
7. **같은 사진에 대해 일관된 점수**
8. **사용자 수 증가와 AI 서버 비용이 비례하지 않을 것**

핵심 구조는 다음과 같다.

```text
사진
 ↓
로컬 Image Encoder
 ↓
Image Embedding

        ↕ cosine similarity

미리 생성된 Concept Embedding
 ↓
HEAT SCORE
```

---

# 2. 중요한 설계 포인트

SEEK에서는 스마트폰에서 **텍스트 임베딩 모델까지 실행할 필요가 없다.**

게임에서 사용할 개념이 예를 들어 300개라면 서버 또는 개발 환경에서 미리:

```text
"여름"
"겨울"
"자유"
"아침"
"여행"
...
```

을 텍스트 임베딩 모델에 넣고 벡터를 생성해둘 수 있다.

예:

```text
여름
→ [0.123, -0.423, 0.781, ...]

겨울
→ [-0.314, 0.422, -0.103, ...]

자유
→ [0.621, -0.221, 0.515, ...]
```

앱에서는 오늘의 문제에 해당하는 Concept Vector만 받는다.

따라서 스마트폰에는 **이미지 임베딩 모델만 탑재하면 된다.**

이 구조의 장점:

- 앱 모델 크기 감소
- 로컬 추론 속도 증가
- 텍스트 모델 불필요
- 서버 AI 호출 비용 제거
- 한국어 UI와 내부 스코어링 언어 분리 가능

---

# 3. 후보 비교

| 모델 | 상업 사용 | 로컬 실행 | 사진↔텍스트 | 대략적 모바일 크기 | 품질 | SEEK 적합도 |
|---|---|---|---|---:|---:|---:|
| **Nomic Embed Vision v1.5** | ✅ Apache 2.0 | ✅ ONNX | ✅ | INT8 약 96.7MB / Q4 약 62MB | 높음 | **1순위** |
| **TinyCLIP ViT-8M/16** | ✅ MIT | ✅ ONNX | ✅ | INT8 약 24.3MB | 낮음~중간 | **2순위** |
| **SigLIP2 Base** | ✅ Apache 2.0 | 가능 | ✅ | 원본 매우 큼 | 높음 | 후보 |
| **MobileCLIP2-S0** | ❌ 연구 전용 | ✅ | ✅ | Vision 약 43MB | 높음 | 상용 제외 |

---

# 4. 1순위 — Nomic Embed Vision v1.5

현재 SEEK에 가장 적합한 1차 후보.

## 장점

- 이미지 임베딩 전용 모델
- `nomic-embed-text-v1.5`와 같은 임베딩 공간 사용
- Apache-2.0 라이선스
- 공식 ONNX 파일 제공
- INT8 및 Q4 양자화 모델 존재
- 이미지 ↔ 텍스트 cosine similarity 구조에 적합
- 서버 없이 모바일에서 채점 가능

구조:

```text
사진
 ↓
Nomic Embed Vision
 ↓
Image Vector

        ↕ cosine similarity

Concept Vector
 ↓
HEAT SCORE
```

---

# 5. Nomic 모델 크기

공개 ONNX 파일 기준 대략:

```text
FP32     약 374MB
FP16     약 187MB
INT8     약 96.7MB
Q4       약 62MB
```

SEEK에서는 우선 **INT8 약 96.7MB 모델**로 품질을 확인하는 것이 안전하다.

Q4는 더 작지만 임베딩 정확도가 떨어질 가능성이 있으므로 처음부터 사용하지 않는다.

권장 순서:

```text
INT8
 ↓
점수 품질 검증
 ↓
성능 충분
 ↓
Q4 테스트
 ↓
품질 차이가 적으면 Q4 고려
```

---

# 6. Nomic의 예상 역할

앱에서는 이미지 인코더만 사용한다.

예:

```text
사진
"아이스크림"

 ↓

Nomic Vision

 ↓

[0.21, -0.44, 0.83, ...]
```

오늘의 개념이 `여름`이면 미리 만들어둔:

```text
summer prototype
→ [0.18, -0.39, 0.79, ...]
```

와 cosine similarity를 계산한다.

예:

```text
cosine_similarity = 0.82

 ↓

Calibration

 ↓

HEAT SCORE = 92.4
```

---

# 7. 한국어 처리 방식

Nomic의 기본 언어 특성이 영어 중심이라도 SEEK에는 큰 문제가 되지 않는다.

사용자에게는:

```text
오늘의 정답
여름
```

이라고 표시하면서 내부적으로는 영어 Concept Prototype을 사용할 수 있다.

예:

```text
UI Label
여름

Internal Concept
summer_001

Prototype Prompts
- summer
- hot summer weather
- summer vacation
- sunny summer outdoors
- warm summer day
```

각 문장을 임베딩한 뒤 평균 또는 가중 평균을 만들어 Concept Prototype으로 사용한다.

사용자가 최종 정답에 `여름`을 입력하는 것은 AI가 아니라 문자열 alias 판정으로 처리한다.

---

# 8. Concept Prototype 권장 방식

단일 단어 하나만 임베딩하면 개념 표현이 너무 좁을 수 있다.

따라서 하나의 정답에 여러 텍스트 프롬프트를 사용한다.

예:

```text
Concept: 여름

summer
hot summer weather
summer vacation
bright summer sunlight
summer beach
warm summer day
```

각 프롬프트:

```text
Text Embedding
 ↓
벡터
```

이들을 평균:

```text
Concept Prototype
=
mean(
  embedding_1,
  embedding_2,
  embedding_3,
  ...
)
```

이 방식으로 개념의 의미 범위를 넓힌다.

---

# 9. 2순위 — TinyCLIP

앱 크기를 극단적으로 줄이고 싶을 경우 유력한 후보.

## 장점

- MIT 라이선스
- 상업 사용 가능
- 이미지 ↔ 텍스트 임베딩 지원
- 모바일용 소형 ONNX 존재
- INT8 약 24.3MB
- 앱 용량 부담이 매우 작음

모델 예:

```text
TinyCLIP-ViT-8M-16-Text-3M
```

대략:

```text
FP32     약 94MB
FP16     약 47MB
INT8     약 24.3MB
```

---

# 10. TinyCLIP의 단점

가장 작은 TinyCLIP은 성능이 낮을 가능성이 있다.

SEEK는 단순 객체 분류 게임이 아니다.

예:

```text
개
고양이
자동차
```

만 구분하면 되는 것이 아니라:

```text
여름
자유
외로움
기다림
휴식
긴장
```

같은 추상 개념도 평가해야 한다.

따라서 TinyCLIP이:

```text
Easy Concept
여름
학교
바다
운동
```

에서는 충분하더라도

```text
Hard Concept
자유
외로움
희망
혼란
```

에서는 점수 품질이 떨어질 수 있다.

반드시 실제 데이터 테스트가 필요하다.

---

# 11. TinyCLIP 채택 조건

같은 사진 데이터셋을 Nomic과 TinyCLIP에 넣고 비교한다.

예:

```text
사진 1,000장

Concept 20개

Nomic Score
vs
TinyCLIP Score
vs
Human Rating
```

다음 조건이면 TinyCLIP 채택 가능:

```text
TinyCLIP 품질
≈
Nomic 품질

차이가 작음
+
모바일 추론 속도 좋음
+
앱 크기 크게 감소
```

그렇다면 약 24MB라는 장점이 매우 커진다.

---

# 12. SigLIP2

기술적으로 매우 좋은 후보이지만 MVP 기준으로는 우선순위가 낮다.

## 장점

- Google
- Apache-2.0
- 이미지 ↔ 텍스트 임베딩
- 높은 성능

## 단점

원본 체크포인트가 매우 크다.

```text
SigLIP2 Base
≈ 1GB 이상 규모
```

따라서 모바일 앱에서 사용하려면:

```text
Text Encoder 제거
 ↓
Vision Encoder 추출
 ↓
Quantization
 ↓
ONNX 변환
 ↓
모바일 최적화
```

과정이 필요하다.

이미 모바일용 공식 양자화 모델이 있는 Nomic에 비해 초기 개발 비용이 크다.

따라서:

```text
Nomic 품질 부족
 ↓
SigLIP2 최적화 검토
```

순서가 적합하다.

---

# 13. MobileCLIP2

기술적으로는 SEEK에 매우 적합하다.

장점:

- 모바일 최적화
- 좋은 이미지-텍스트 임베딩 성능
- 작은 모델
- 빠른 추론

하지만 상용 앱에는 사용하지 않는 것이 안전하다.

현재 Apple 연구 모델 라이선스는 비상업 연구 목적 중심이며 commercial product/service 사용에 제약이 있다.

따라서:

```text
연구용 POC
가능

상용 SEEK 앱
제외
```

로 정리한다.

---

# 14. 모바일 런타임

권장 런타임:

## ONNX Runtime Mobile

하나의 ONNX 모델을 iOS / Android에서 공통으로 사용할 수 있다.

구조:

```text
             SEEK App
                │
       ONNX Runtime Mobile
          ┌─────┴─────┐
          │           │
       Android       iOS
          │           │
        NNAPI       CoreML
        XNNPACK     XNNPACK
```

장점:

- 동일 모델 사용
- 점수 일관성 확보
- Android / iOS 코드 통합 용이
- CPU/NPU/GPU 가속 가능

---

# 15. iOS / Android 점수 일관성

SEEK는 랭킹 게임이므로 플랫폼마다 다른 모델을 쓰면 안 된다.

예:

```text
iOS
Model A
→ 92점

Android
Model B
→ 87점
```

이런 구조는 공정하지 않다.

따라서 반드시:

```text
Same Model
Same Preprocessing
Same Embedding Dimension
Same Cosine Formula
Same Calibration
```

을 유지한다.

---

# 16. 사진 전처리

모든 기기에서 동일하게 처리해야 한다.

예:

```text
Camera Image
 ↓
Center Crop / Resize
 ↓
224 × 224
 ↓
RGB Normalize
 ↓
Image Encoder
```

전처리 차이가 생기면 embedding 결과도 달라질 수 있다.

따라서 전처리 코드도 플랫폼 공통 규칙으로 고정한다.

---

# 17. 서버 역할

로컬 AI를 사용하더라도 서버는 필요하다.

서버가 담당할 것:

```text
오늘의 Puzzle ID
Concept Metadata
Concept Embedding
Score Calibration
Streak
Leaderboard
Friend Data
Game Config
Model Version
```

사진 자체는 기본적으로 서버에 올리지 않는다.

---

# 18. 앱 다운로드 구조

모델을 앱 바이너리에 직접 넣을 수도 있고 첫 실행 시 다운로드할 수도 있다.

## 방식 A — 앱 포함

```text
앱 설치
+
Model 97MB
```

장점:

- 즉시 실행
- 네트워크 불필요

단점:

- 앱 용량 증가

## 방식 B — 최초 실행 다운로드

```text
앱 설치
↓
Model Download
↓
로컬 저장
```

장점:

- 스토어 초기 다운로드 크기 감소
- 모델 교체 쉬움

단점:

- 첫 실행 UX 필요

SEEK에서는 모델 업데이트 가능성을 고려하면 **초기 다운로드 방식**도 충분히 검토할 가치가 있다.

---

# 19. 모델 버전 관리

서버에서 현재 점수 모델 버전을 관리한다.

예:

```text
model_version
seek-embedding-v1.0

calibration_version
seek-calibration-v3
```

랭킹 시즌 도중 모델을 바꾸지 않는다.

모델이 변경되면:

```text
Season 1
Model v1

Season 2
Model v2
```

처럼 구분하는 것이 공정하다.

---

# 20. 점수 계산

기본:

```text
image_embedding
concept_embedding
```

두 벡터의 cosine similarity를 구한다.

공식:

```text
cosine_similarity
=
(A · B)
/ (||A|| × ||B||)
```

이 결과를 바로 사용자에게 보여주지 않는다.

예:

```text
Raw Similarity
0.57
```

를 calibration을 통해:

```text
HEAT SCORE
89.4
```

로 바꾼다.

---

# 21. Calibration

모델에서 나오는 similarity는 concept마다 분포가 다를 수 있다.

예:

```text
여름
관련 사진 평균 similarity
0.55

자유
관련 사진 평균 similarity
0.41
```

같은 0.50이라도 의미가 다를 수 있다.

따라서 개념별 점수 분포를 만든다.

예:

```text
Concept: 여름

0.30 → 10점
0.40 → 35점
0.50 → 66점
0.60 → 88점
0.70 → 97점
```

이렇게 percentile 기반 점수 변환을 사용한다.

---

# 22. 인간 평가 데이터 필요

최종 게임 품질은 모델 벤치마크보다 사람의 직관이 중요하다.

필요한 데이터셋 예:

```text
Concept 20개

각 Concept별
관련 사진 25장
중립 사진 25장
무관 사진 25장
```

총:

```text
20 × 75
=
1,500장
```

정도부터 테스트 가능.

사람 평가:

```text
이 사진은 "여름"을 얼마나 잘 표현하는가?

1
2
3
4
5
```

를 수집한다.

그 다음:

```text
Model Ranking
vs
Human Ranking
```

을 비교한다.

---

# 23. POC 추천 개념

처음에는 20개 정도.

## Easy

```text
여름
겨울
바다
학교
운동
비
아침
음식
여행
파티
```

## Medium

```text
휴식
건강
속도
공부
따뜻함
위험
축하
질서
모험
기다림
```

## Hard

추후:

```text
자유
외로움
희망
혼란
긴장
추억
```

---

# 24. POC 모델 비교

권장 실험:

```text
Dataset
1,000 ~ 1,500 images

Models
Nomic INT8
TinyCLIP INT8
```

측정 항목:

```text
Human Correlation
Top-K Accuracy
Score Stability
Inference Latency
RAM Usage
Battery Impact
Model Size
Crash Rate
```

---

# 25. 목표 latency

게임 UX상 사진 촬영 후 점수가 너무 늦게 나오면 안 된다.

목표:

```text
사진 촬영
↓
1초 이내
↓
🔥 91.4
```

이상적:

```text
300~700ms
```

구형 기기:

```text
1~2초 허용
```

---

# 26. 앱 원가 구조

로컬 임베딩 구조의 가장 큰 장점.

```text
사진 1장
→ AI 서버 비용 0원

사진 10장
→ AI 서버 비용 0원

DAU 1,000
→ AI 서버 비용 0원

DAU 100,000
→ AI 서버 비용 0원
```

사용자 증가와 inference 비용이 분리된다.

서버 비용은:

```text
DB
Leaderboard
Analytics
Remote Config
Auth
```

등 일반 서비스 비용 중심이 된다.

---

# 27. 서버 AI 사용 위치

서버 생성형 AI는 핵심 스코어링에 사용하지 않는다.

다음과 같은 부가 기능에서만 사용한다.

예:

```text
"왜 이 사진이 82점이야?"
```

↓

```text
광고 시청
또는
Premium
```

↓

```text
Server AI
```

이런 구조라면:

```text
수익 발생
↓
AI 비용 발생
```

순서가 가능하다.

---

# 28. 치팅 문제

로컬 점수 계산은 조작 가능성이 있다.

예:

```text
score = 99.9
```

를 해킹해서 서버에 전송할 수 있다.

따라서 일반 사용자는 로컬 점수를 사용하되:

```text
Top 1%
연속 99점
세계 랭킹 진입
비정상 플레이
```

같은 경우에만 서버 검증한다.

서버 검증은 소수 사용자에게만 적용한다.

---

# 29. 최종 권장안

현재 기준:

## 1순위

```text
Nomic Embed Vision v1.5
INT8
약 96.7MB
```

이유:

- 상업 사용 명확
- 공식 ONNX
- 적당한 모델 크기
- 품질 우수
- 이미지↔텍스트 embedding 구조
- SEEK 목적에 가장 가까움

## 2순위

```text
TinyCLIP
INT8
약 24.3MB
```

이유:

- 매우 작음
- 빠름
- MIT
- 모바일 탑재 용이

단점:

- 추상 개념 점수 품질 검증 필요

---

# 30. 최종 선택 전략

처음부터 하나를 확정하지 않는다.

다음 순서로 진행한다.

```text
Nomic INT8
        │
        ├──────────┐
        │          │
        ▼          ▼
품질 평가       속도 평가

TinyCLIP INT8
        │
        ├──────────┐
        │          │
        ▼          ▼
품질 평가       속도 평가
```

동일 이미지 데이터셋으로 비교.

결과:

```text
TinyCLIP 품질 ≈ Nomic
→ TinyCLIP 선택

Nomic 품질 >> TinyCLIP
→ Nomic 선택
```

---

# 31. 권장 기술 스택

```text
Mobile
Flutter / Native

AI Runtime
ONNX Runtime Mobile

Image Model
Nomic Embed Vision v1.5 INT8
또는
TinyCLIP INT8

Server
Firebase

DB
Firestore

Auth
Firebase Auth

Analytics
Firebase Analytics

Remote Config
Firebase Remote Config

Ads
AdMob
```

---

# 32. 최종 아키텍처

```text
                SEEK APP

                   │
                   ▼

               Camera

                   │
                   ▼

          Image Preprocessing

                   │
                   ▼

        Local Embedding Model

                   │
                   ▼

            Image Vector

                   │
              cosine similarity
                   │

          Concept Vector
          (Firebase 제공)

                   │
                   ▼

             Calibration

                   │
                   ▼

            HEAT SCORE

                   │
           ┌───────┴────────┐
           │                │
        Local UI         Firebase
                       결과/랭킹만
```

---

# 33. 결론

SEEK의 장기 구조는 **로컬 이미지 임베딩 모델 + 미리 생성된 Concept Embedding** 조합이 가장 적합하다.

핵심 원칙:

> **플레이 횟수가 증가해도 AI 서버 비용이 증가하지 않아야 한다.**

현재 가장 유력한 1차 모델은:

```text
Nomic Embed Vision v1.5 INT8
```

비용/용량을 더 줄일 수 있는 후보는:

```text
TinyCLIP INT8
```

이다.

다음 단계는 모델을 더 조사하는 것이 아니라 **동일 사진 데이터셋을 두 모델에 넣어 실제 사람의 판단과 얼마나 일치하는지 측정하는 POC**다.

최종적으로 SEEK가 성공하려면 가장 중요한 것은 벤치마크 점수가 아니라:

> **사용자가 “이 점수는 납득된다”고 느끼는가**

이다.
